dataset:
  dataset_dir: ~/.torch/datasets/CIFAR10
model:
  name: resnet
  resnet:
    depth: 50
train:
  small_train: True
  output_dir: experiments/cifar10/exp00
  checkpoint_period: 100
scheduler:
  epochs: 100
augmentation:
  use_ricap: True